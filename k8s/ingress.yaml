apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: contact-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: contact-api
                port:
                  number: 2536
